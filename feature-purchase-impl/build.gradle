import com.temq.dev.GradleInterop

GradleInterop.addBazelInterop(project, "feature-purchase-impl")

//todo move it to the interop method, exclude main src folder
sourceSets {
    main {
        java {
            exclude("com")
        }
    }
}